@{
    var chart = new Chart(width: 450, height: 450, theme: ChartTheme.Vanilla)

.AddSeries("Default", chartType: "Pie",
    xValue: Model, xField: "Breed",
    yValues: Model, yFields: "TotalSheep")

.Write(format: "png");
   
}
